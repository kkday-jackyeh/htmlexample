---
kind: pipeline
type: docker
name: default

steps:
  - name: cache
    image: redis
    detach: true

  - name: ping
    image: redis
    commands:
      - sleep 5
      - redis-cli -h cache ping
# services:
#   - name: cache
#     image: redis
